$C = "../Common.ui";

Group #ArenaListPage {
    $C.@PageOverlay {}

    $C.@DecoratedContainer {
        @CloseButton = true;
        Anchor: (Width: 640, Height: 480);

        Group #Title {
            $C.@Title #TitleLabel {
                @Text = "Arena Management";
                Anchor: (Top: 12, Height: 24);
            }
        }

        Group #Content {
            Padding: (Full: 20);
            Anchor: (Top: 40, Bottom: 70);
            LayoutMode: Top;

            // Top row: Create button + filter dropdown
            Group #TopRow {
                Anchor: (Height: 40);
                LayoutMode: Left;

                $C.@TextButton #CreateNewBtn {
                    @Text = "Create New Arena";
                    @Anchor = (Width: 200, Height: 40);
                }

                Group { FlexWeight: 1; }

                Label {
                    Text: "Filter:";
                    Anchor: (Width: 46, Height: 40);
                    Style: (FontSize: 12, TextColor: #6b7d8e, VerticalAlignment: Center);
                }

                $C.@DropdownBox #FilterDropdown {
                    Anchor: (Width: 180, Height: 32);
                    Style: (
                        ...@DefaultDropdownBoxStyle,
                        SearchInputStyle: DropdownBoxSearchInputStyle(
                            Anchor: (Height: 20),
                            Padding: (Left: 28, Right: 24),
                            PlaceholderText: %server.customUI.searchPlaceholder,
                            PlaceholderStyle: (TextColor: #3d5a85, RenderUppercase: true, FontSize: 14),
                            Icon: (Texture: "Common/SearchIcon.png", Width: 16, Height: 16, Offset: 9),
                            ClearButtonStyle: $C.@ClearButtonStyle
                        )
                    );
                    ShowSearchInput: true;
                }
            }

            // Arena count label
            Label #ArenaCountLabel {
                Text: "0 arenas loaded";
                Anchor: (Height: 16, Top: 4);
                Style: (FontSize: 11, TextColor: #6b7d8e);
            }

            // Scrollable arena list
            Group #ArenaList {
                Anchor: (Top: 6);
                FlexWeight: 1;
                LayoutMode: TopScrolling;
                ScrollbarStyle: $C.@DefaultScrollbarStyle;
            }
        }

        // Bottom Button Row
        Group #ButtonRow {
            Anchor: (Bottom: 16, Height: 44, Left: 20, Right: 20);
            LayoutMode: Left;

            $C.@SecondaryTextButton #BackBtn {
                @Text = "Back";
                @Anchor = (Width: 120);
            }

            Group { FlexWeight: 1; }

            $C.@SecondaryTextButton #SyncChunksBtn {
                @Text = "Sync Chunks";
                @Anchor = (Width: 140);
            }
        }
    }
}
